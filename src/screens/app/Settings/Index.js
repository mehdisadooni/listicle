import React from "react";
import {Text, View} from "react-native";

const Settings = () => {
    return (
        <View>
            <Text>Settings</Text>
        </View>
    )
}

export default React.memo(Settings)